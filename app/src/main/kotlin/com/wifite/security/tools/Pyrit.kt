package com.wifite.security.tools
import timber.log.Timber

class Pyrit {
    fun stripHandshake(cap: String) = Timber.d("Strip handshake")
    fun exportPcap(output: String) = Timber.d("Export pcap")
}
